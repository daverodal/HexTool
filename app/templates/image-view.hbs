{{#if view.isLoading}}
    <h1>LOADING BE PATIENT !!!</h1>
{{else}}
{{/if}}
<div class="tutWrapper">

    <img class="mapImage" {{bind-attr src="mapUrl" }} {{bind-attr style="mapWidthStyle"}}>
    <canvas id="tutorial" {{bind-attr src="coNam" width="imageWidth" height="controller.imageHeight"}}></canvas>

</div>