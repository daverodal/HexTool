import { test, moduleFor } from 'ember-qunit';
import TerrainProperties from '../../../models/terrain-properties';

moduleFor('model:terrain-properties', ' Try to prove object is object ',{

});
test('model exists', function() {
    expect(0);
//    ok(this.subject() instanceof TerrainProperties, 'the model exists');
});